import React from 'react';

export default class About extends React.Component {
  render(): React.ReactNode {
    return (
      <div>
        About
        {/* <button onClick={() => this.navigate('/')}>Go Back Home Page</button> */}
      </div>
    );
  }
}
